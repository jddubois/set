{
  "name": "set",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"cd client && npm start\" \"cd server && npx nodemon src/index.ts\""
  },
  "devDependencies": {
    "@types/node": "^15.0.2",
    "body-parser": "^1.19.0",
    "concurrently": "^4.0.1",
    "express": "^4.17.1",
    "ts-node": "^9.1.1"
  },
  "dependencies": {
    "prettier": "^2.2.1"
  }
}
